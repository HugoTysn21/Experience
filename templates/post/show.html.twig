{% extends 'base.html.twig' %}

{% block title %}Post{% endblock %}

{% block body %}
    <h1>Post</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ post.id }}</td>
            </tr>
            <tr>
                <th>CreatedBy</th>
                <td>{{ post.createdBy }}</td>
            </tr>
            <tr>
                <th>Title</th>
                <td>{{ post.title }}</td>
            </tr>
            <tr>
                <th>Introduction</th>
                <td>{{ post.introduction }}</td>
            </tr>
            <tr>
                <th>CreatedAt</th>
                <td>{{ post.createdAt ? post.createdAt|date('Y-m-d H:i:s') : '' }}</td>
            </tr>
            <tr>
                <th>IntroductionPost</th>
                <td>{{ post.introductionPost }}</td>
            </tr>
            <tr>
                <th>ContentTwo</th>
                <td>{{ post.contentTwo }}</td>
            </tr>
            <tr>
                <th>ContentThree</th>
                <td>{{ post.contentThree }}</td>
            </tr>
            <tr>
                <th>ContentFour</th>
                <td>{{ post.contentFour }}</td>
            </tr>
            <tr>
                <th>AvatarDirectory</th>
                <td>{{ post.avatarDirectory }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('post_index') }}">back to list</a>

    <a href="{{ path('post_edit', {'id': post.id}) }}">edit</a>

    {{ include('post/_delete_form.html.twig') }}
{% endblock %}
